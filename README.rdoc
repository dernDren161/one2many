== README

one2many is a web application that let's users store/list in all their important digital data under one directory and have easy access to it.
Things you may want to cover:

* Ruby version -- ruby 2.5.1p57

* Database creation -- The application uses MYSQL for database storage and management.

* Database initialization -- Credentials for database access: ( username: one2many, password: may2one )

* Run -- bundle install
